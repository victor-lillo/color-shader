export interface Test {
  text: string
  name: string
  arrayNum: number[]
}

export interface Post {
  userId: number
  id: number
  title: string
  body: string
}
export interface ColorObject {
  dark: string[]
  light: string[]
  opacity: string[]
}
